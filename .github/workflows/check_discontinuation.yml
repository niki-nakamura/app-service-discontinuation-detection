name: Check App Discontinuation
on:
  schedule:
    - cron: '0 2 * * *'     # 毎日 02:00 UTC（日本時間 11:00）に実行
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # → ここに後で「config/apps.yml を読み込んで検査スクリプトを実行」ステップを追加
