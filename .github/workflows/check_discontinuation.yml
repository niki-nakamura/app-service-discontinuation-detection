name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # ※言語を決めたら以下をコメントアウト解除して利用
      #- name: Setup Python
      #  uses: actions/setup-python@v4
      #  with:
      #    python-version: '3.x'

      #- name: Install dependencies
      #  run: pip install -r requirements.txt

      #- name: Run tests
      #  run: pytest --maxfail=1 --disable-warnings -q

      - name: Placeholder step
        run: echo "後ほど言語を決めて install や test をここに追加します"
